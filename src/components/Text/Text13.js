const Text13 =({color,title})=>{
    return (
        <span className={`text-xs leading-[15px] lg:text-[11px] 2xl:text-xxs lg:leading-4 font-inter font-semibold text-${color}`}>{title}</span>
    )
}

export default Text13;