const TextBase =({title})=>{
    return (
        <span className="font-inter font-semibold text-xs lg:text-base leading-5 text-white inline-block truncate">
            {title}
        </span>
    )
}

export default TextBase;