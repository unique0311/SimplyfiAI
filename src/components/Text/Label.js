
const LinkLabel =({title})=>{
    return (
        <label className="cursor-pointer text-sm leading-[17px] text-primary font-inter inline-block">
            {title}
        </label>
    )
}

export default LinkLabel;